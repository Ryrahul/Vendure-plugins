/**
 * Configuration options for FaqPlugin.
 * Currently empty — reserved for future options.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface FaqPluginOptions {}

// --- GraphQL input/args types are generated via codegen ---
// Import from '../gql/generated' instead of defining manually.
// To regenerate: cd apps/dev-server && npm run schema && npm run codegen
